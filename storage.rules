rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Evidence is uploaded through short-lived server-authorized signed URLs.
    // No browser identity, including ADMIN, may read/write Storage directly.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
